* Fetch full table?
  more than 200 PRs.

* Totals to spreadsheet.
  > make total fields calculated?

* temp popup when copy to clipboard clicked
* Solucious: (dit zijn zoekresultaten van artikels – is geen form!
  Nt alcohol:
  https://s1-eu.ariba.com/gb/search/search-result/explore/%22SO2022_1329X%20,%22?realm=744379882-C1
  alcohol
  https://s1-eu.ariba.com/gb/search/search-result/explore/%22SO2022_1329D%20,%22?realm=744379882-C1
  Slechts 1 INPUT veld.
  Geen plaats voor netto/bruto
  Search results are list row with up to 3 items?
  Desnoods netto/bruto onder elkaar?
  Of net naast elkaar met hoofding on top en BTW daaronder? Niet onder want daar staat calcResult, dus erboven.
  Bwa…toch onder elkaar, dan kan het veld langer voor berekeningen.

  >>> kopieer de berekeningen naar winkelmandje en form (maar…Bahati voegt soms hetzelfde artikel 2x toe)
  	Hoe link je een nieuwe lijn aan winkelmandjes lijn? Wel, shoppingCart kan worden opgevraagd, niet? Dus weet je wat de volgende lijn is.
  	Maar…als iemand een lijn verwijderd klopt de index niet meer….


  How to detect if a shoppingCart line is deleted…and which one?
  	Add extra listeners?
  	No captured indexes as these will shift when deleting!

* bruto/netto, when copied from q field: consider a dot a thousands point!
* 1280,00€ : euro at the end.
  > also as decimal?

* error
index.js:125 Uncaught (in promise) DataError: Failed to execute 'put' on 'IDBObjectStore': Evaluating the object store's key path did not yield a value.
    at Proxy.<anonymous> (index.js:125:26)
    at Proxy.method (index.js:238:20)
    at set (gringoDb.ts:26:29)Understand this error
* check of er een bijlage is en gedeeld met team!
* popup tags: a bit more opacity for the non-selected ones


* Ouders mail sturen dat initiatie terug open staat.
  > eerst nog ff testen...

* PR48767: lots of items, only the 1st has bruto/netto.

* Test case undefined BTW tarif: > Eten > luxe visproducten.

* budget depends on grants? Include them in budget key.
  > projects don't, so this is less critical or urgent?

* quantity field updates the server on blur. Postpone the blur until netto or bruto are blurred to avoid server roundtrip on each character.

Excel reqs
===========
Have overview of allocated and spent budget.
  > needs to be updated from 2 sources:
    - allocated: source? Bahati overview?
      > likely manual update.
    - spent: ariba?
      > excelscript button?
        > fetch is possible.
          > ariba totals can be on cloud.

* Totals groups: within group:
  ** have totals per tag?
      > tags are not mutually exclusive.
        > even MW/BK are not always exclusive: some costs are for both.
          > so...
            > group for the selected tags.
              > remember settings.
      > also allow grouping per project within budget?
  ** or split price column into multiple columns?
      > how many?
        > User defined?
          > a column can have a source or definition or formula
            : `=mw+bk` : arithmetic
            : `mw+bk` : has mw AND bk
            : `mw,bk` : has mw OR bk
            does not work with projects, unless...
            : `'My project', 'Your project'` : has mw OR bk

            > title of column?
              : `mw,bk "titel"` : has mw OR bk

        > essentially only mw/bk?
        > but..."didactische materiaal per project?"

* Be able to quickly set project for PR when it's missing, using a link in the overview...or in  the overview itself.

* Req list: when only one page, navivation has no input element and prs don't get decorated.

* Make totals overview much like the excel for easy comparison of bulk copying.

Expand settings
=================
* Add local colors to settings
* Add options to (de)activate certain features.
  - tags
  - projects
  - bruto
  - calculator
  > all of these are opt-in.



Decorate price
===============
* remove dot and comma on paste intelligently
  > right-most indicator wins.

* Add calculator buttons.
  > fill in the bruto amount and press "-21%"
  > allow simple arithmetic in field








* Totals and exports: exclude non active statusses (rejected, 'In aanmaak', ...)

To Excel...
=============
> How to copy lines to excel?
> How to compare with excel?
Excel MUST have a prId.
Excel online allows power query!!!

  1. Export data as html table. (see dko3 plugin: table to excel)
    > export the PR Lines.
    :     navigator.clipboard.writeText(createHtmlTable(headers, cols).outerHTML).then(_r => {});
  2. Paste it in an excel tab.
  3. Have power queries fill the other tabs.
     > User data?
       > Have a section below each generated table for user data? and have a sum below it?

Difficult to merge user added lines with copied lines.
Start from a simple data dump.
Aggregate


Aggregates UX
==============
You need to be signed in, so not a options page.
A button on the Requestlist page...
An extra tab?



* Create a page to show the sum per project.
* Create a page to sow the sum per budgetPos ( != grootboekrekening!)
  > create a list of grootbookrek, budgetPos

* To list tarifs(via commodity-codes)
  >>> https://s1-eu.ariba.com/gb/tenant/744379882-C1/user/.../resource/container/7bebfe6a-297c-486c-8dea-e6fc90d0a240?browserRequestId=Tile1779715254146&pu=PU_AG+Stedelijk+Onderwijs&puHierarchyPath=PU_AG+Stedelijk+Onderwijs,All
    > where previous tile/page = 7bebfe6a-297c-486c-8dea-e6fc90d0a240
  - the pre-form data is fetched with:
  https://s1-eu.ariba.com/gb/tenant/744379882-C1/user//resource/container/48a90905-23a9-4c34-bce1-c968edfeee95?browserRequestId=Tile1779715762838&pu=PU_AG+Stedelijk+Onderwijs&puHierarchyPath=PU_AG+Stedelijk+Onderwijs,All
    > where tileId from previous fetch = 48a90905-23a9-4c34-bce1-c968edfeee95

* Add BTW buttons next to price field to subtract BTW.
  > already do this in pre-screen?
    > do we have the commodityCode?
      if yes, display it.

* Decorate article form (before PR)
 - Add BTW calculator.

* Unique name for article: Description.CommonCommodityCode
  > add this code to the excel...
  > ...or a more flexible way?
    > excel is NOT up-to-date.
    > We don't need ALL the codes


* add button to copy line to excel.
  > perhaps add prId to excel?

* Add setup for meta
  - local setup:
    - local tags with colors
    - custom colors for global tags
    - gear button for global setup with:
      - warning in big red box.
      - Project lines
      - Global tags

Priorities
===========
* Copy a 'line' for excel.
* Create summary of all PRs




Tags maintenance
=================
CRUD
----
C: OK: hardcoded and global for now: keep colors modest.
R: OK: only global for now: add local colors later.
U: OK: can be created when needed.
D: OK: orphans shown.


* Standard tags
  - MW, BK, BB>, V,
  > set by whom? Me, I guess...

* Personal tags
  > store where?
Delete
------
* Tag stayes attached to the item, but has no color and cannot be filtered.
  > test!
Update
------
* The rename needs to propagate to all users and local caches.
  > if you do the rename in all cloud files that contain the tag, the caches are implicitly invalidated. (easier than I thought...)
    > this cloud function can be created when needed.
Customize
---------
User can set local custom colors.


* Tag filters, when collapsed you have no idea what is shown or not
  > summarise them on tag line.

* Already create a total list per ledger.
  > separate page?
  > we need to be in the Aanvragen...or...do we?
    > does the fetch work from any page?


* When doing an ariba filter: Tag filter disappears.


Meta sync
===========
  > keep a local last_sync z-timestamp
  > fetch all tags since.
    > create a cloud function.
    > when?
      at startup and...
      > every x time?
        5 min ok?
  Currently, when no last-sync date is present, all local metas are removed and last-sync date is set to now.
  This forces a read of every single meta to be fetched separately.
  On subsequent fetchChangedSince() calls, only the most recently changed metas are fetched.
  > Is this waterproof?



Filters
===========
* I want to see the PRs that are
  -  not BK <or> MW
    > currently NOT possible!
      > not the end of the world because NOT BK is more usefull.
  -  written by me <or>  Bahati


* Available tags
  > personal, general or both?
    > personal only visible to user
    > UI difference? italic? border?
    > global: start with hard-coded list.



Goals
==========
* Reduce work to copy pr/order to Excel...or...
* Replace Excel?
  > requires assigning a project to each pr.
    >  Perhaps only assign projects for MW prs.
    > Assign project in pr, before copying to Excel.
  > this disables alterations to excel data.

Data exchange
=============
* Plugin should be able to login with SSO to sharepoint.
  > "how to use sso in a chrome plugin"
    > enable 'identity' permission in manifest.json
    > Microsoft Enterprise SSO: On managed Windows/macOS, use the Microsoft Single Sign-On extension for Chrome to enable seamless authentication with Entra ID (formerly Azure AD) [5.2, 5.9].

meta UI.
=========
 - tags is list of strings, icons.
 - add: button with popup.
 - popup:
   > how to show a tag is selected?
     > the selected ones on top
     > highlight?
       > background color?



* Make list item hiding user options.
  > local first with cloud backup.
    > user in footer
