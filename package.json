{
  "name": "gringo",
  "version": "1.0.0",
  "description": "This extension only works within SO.",
  "main": "typescript/plugin_options/options_page.ts",
  "scripts": {
    "test": "echo \"Note that the build command does a TSC first for stricter compilation.\" && exit 1",
    "build_all": "npm run build_content && npm run build_options && npm run build_serviceworker",
    "build_all_quick": "rolldown -c rolldown_all.config.js",
    "build_content": "tsgo --build &&  rolldown -c",
    "build_serviceworker": "tsgo --build &&  rolldown -c rolldown_service.config.js",
    "build_options": "tsgo --project ./tsconfig_options.json && rolldown -c rolldown_options.config.js"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/chrome": "^0.0.313",
    "rolldown": "^1.0.0-beta.8"
  }
}
